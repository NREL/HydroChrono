hydrodynamics:
  bodies:
    - name: body1
      h5_file: hydroData/iea_sphere.h5

  waves:
    # type: regular
    # height: 2.5
    # period: 8.0
    # direction: 0.0  # degrees, 0 = positive x
    # phase: 0.0
    type: still

    # Alternative 1:
    # type: no_wave

    # Alternative 2 (equivalent to still via zero amplitude):
    # type: regular
    # height: 0.0
    # period: 8.0